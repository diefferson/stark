name: stark
description: Stark - a pragmatic lightweight dependency injection framework for Dart
version: 4.0.0
homepage: https://github.com/diefferson/stark

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.0.0

flutter:
